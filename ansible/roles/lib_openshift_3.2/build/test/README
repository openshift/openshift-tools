After generate.py has run, the ansible modules will be placed under openshift-tools/ansible/roles/lib_openshift_3.2/library.


To run the tests you need to run them like this:
./services.yml -M ../../library
